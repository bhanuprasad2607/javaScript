// Arrow Functions 
    // Function Declaration in JavaScript

function simpleFunction(name){
    console.log(name);
}

simpleFunction('Hooks is in Simple Function Declaration');


    // Arrow function Declaration in JavaScript 

const mySimpleFunc = (name)=>{
    console.log(name);
}

mySimpleFunc("Hooks in Arrow Function Declaration")

    // Other methods for Declaring the Arrow method in JavaScript.

const mySimpleFunction = name =>{
    console.log(name);
}

mySimpleFunction('Hooks with other method of Declaration in JavaScript');


    // Declaration of arrow Function in javaScript in Single line

const singleLineArrow = name => console.log(name);

singleLineArrow('Hooks in Arrow Function Declaration in single Line javaScript');
